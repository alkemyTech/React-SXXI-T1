const FILE_SIZE = 200 * 1024;
const SUPPORTED_FORMATS = ["image/jpg", "image/jpeg", "image/png"];

const imageSchema = { downloadImage: "Cargar Imagen" };

export { FILE_SIZE, SUPPORTED_FORMATS, imageSchema };
